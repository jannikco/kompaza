# Application
APP_NAME="Kompaza"
APP_URL=http://localhost:8000
APP_ENV=development
APP_DEBUG=true
PLATFORM_DOMAIN=kompaza.com

# Database
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=kompaza
DB_USERNAME=kompaza
DB_PASSWORD=your_password_here

# Brevo Email Service (platform default)
BREVO_API_KEY=
BREVO_LIST_ID=
MAIL_FROM_ADDRESS=info@kompaza.com
MAIL_FROM_NAME="Kompaza"

# Stripe (platform billing)
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CONNECT_WEBHOOK_SECRET=

# Impersonation
APP_SECRET=random-secret-here

# OpenAI (for AI-powered lead magnet generation)
OPENAI_API_KEY=

# Security
CSRF_TOKEN_NAME=csrf_token

# Cron secret — required to call /api/cron/* endpoints (abandoned carts, email sequences,
# membership events). Generate with: php -r "echo bin2hex(random_bytes(32));"
# Call crons as: curl -s "https://<tenant>.kompaza.com/api/cron/<job>?key=$CRON_SECRET"
CRON_SECRET=

# Superadmin
SUPERADMIN_EMAIL=admin@kompaza.com
